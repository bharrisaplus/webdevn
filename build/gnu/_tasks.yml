# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  rear-x64:
    env:
      BUILD_OUTPUT_DIR: '{{.BUILD_ROOT}}/gnu/x64'
    cmds:
      - echo "Hey, it's gnu here"
